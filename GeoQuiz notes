select quiz from dropdown
	jquery:
		zoom map to area
			set map position (again - should be same as the zoom)
				#world_map_div left: -750px; top: -230px;
				europe top 70 left-750
				zoom: width 1942
		set start quiz button form to corresponding action (africa_quiz for example)
		if Europe, ajax in a 'coming soon' message
		
Start quiz button
	hides continent select menu
	changes url quiz/africa
	view data
		africa quiz question
		form for that quiz (e.g.: formtype = africa)
		set map position (again - should be same as the zoom)
			#world_map_div left: -750px; top: -230px;
			zoom: width 1942
		include image map
		
quiz questions ajaxed to country_process_quiz

In header
	if quiz > add JS code in header
	
	
=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-	
Thurs to do

process for when player isn't registered or logged in

add maps to external file, or db?

sortable score table, or sort by date at least.

clean up code, rename stuff

add flags?

add leichtenstein, turkey, cyprus
=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-

In from codeigniter
	
array	0
			array 0
					Mali
					ml
		1
			array 0
					Mauritania
					mr
					
assign array to doc object

	run question display function
		put country and initials in question area
	
upon url imagemap click

	run question check function
		compare initial to url value
			if match
				display 'correct!' message for a second
				question display()
			else
				display 'try again'
	
		
save score
	sends to quiz/save_score
		post game_id
		post score
		put post data in session
		if not logged in
			go to login
				
		if logged in true
			save score etc.





















		